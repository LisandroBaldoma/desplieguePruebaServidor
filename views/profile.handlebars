<div class="d-flex justify-content-center flex-column align-items-center">
<div class="card text-center col-6 mt-3">
  <div class="card-header">
    PROFILE
  </div>
  <div class="card-body">
    <h5 class="card-title">Datos perfil usuario</h5>
    <p class="card-text">Nombre completo: {{user.name}}, {{user.lastName}}</p>
    <p class="card-text">Correo electronico: {{user.email}}</p>
    <p class="card-text">Rol: {{user.rol}}</p>    
  </div>
  <div class="card-footer text-muted">
    Ultima Conexion: {{user.last_connection}}
  </div>
  
</div>

<form id="formLogout" class="d-flex justify-content-center mt-3">
  <button type="submit" class="btn btn-primary m-2">Logout</button>
  <a class="btn btn-secondary m-2" href="/products" role="button">E-commerce</a>
</form>

</div>


<script src="/static/js/profile.js"></script>